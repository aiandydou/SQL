INSERT INTO "TEP_DETECTOR_FLOW_Yiqing_0223"
SELECT * FROM TBL_DETECTOR_FLOW
WHERE SIGNAL_CROSS_NO IN 
(SELECT DISTINCT SIGNAL_CROSS_NO FROM TBL_DETECTOR_DEVICE 
WHERE LANE_NO IN (
SELECT DISTINCT LANE_NO FROM TBL_LANE
WHERE CROSS_NAME LIKE '%人民路%'));
